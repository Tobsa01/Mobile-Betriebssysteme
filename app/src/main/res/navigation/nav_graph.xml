<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/FirstFragment">

    <fragment
        android:id="@+id/FirstFragment"
        android:name="com.example.mbs_project.android.FirstFragment"
        android:label=""
        tools:layout="@layout/fragment_first">
        <action
            android:id="@+id/action_FirstFragment_to_nextActivity"
            app:destination="@id/nextActivity" />
    </fragment>
    <activity
        android:id="@+id/nextActivity"
        android:name="com.example.mbs_project.android.NextActivity"
        android:label="NextActivity" />
</navigation>